apply plugin: 'android'

dependencies {
    compile project(':programming')
    compile project(':manyquiz')
}

android {
    compileSdkVersion rootProject.compileSdkVersion
    buildToolsVersion rootProject.buildToolsVersion
}
